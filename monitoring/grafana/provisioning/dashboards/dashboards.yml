apiVersion: 1

providers:
  - name: Muse Service
    orgId: 1
    folder: Muse Service
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
